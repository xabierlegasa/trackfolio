# Optional compose env for infra/production (DB bootstrap + front build arg).
# Laravel secrets live in trackfolio-api/.env (Ansistrano shared file).

VITE_API_BASE_URL=http://192.168.1.136:8080

DB_DATABASE=trackfolio
DB_USERNAME=trackfolio
DB_PASSWORD=changeme
DB_ROOT_PASSWORD=changeme

RABBITMQ_USER=trackfolio
RABBITMQ_PASSWORD=changeme
